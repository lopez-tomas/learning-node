//function add( a,b ) {
	//return a + b;
//}

const add = (a, b) => a + b; // implicit return, because it has only 1 line

const greetings = () => 'Hello World';


console.log( add( 5, 10 ) );
console.log( greetings() );
