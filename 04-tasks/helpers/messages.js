const colors = require('../config/colors');

const showMenu = () => {
  console.clear;
  console.log('==================='.header);
  console.log(' Select one option'.header);
  console.log('===================\n'.header);

  console.log(``);
};

module.exports = {
  showMenu
}
